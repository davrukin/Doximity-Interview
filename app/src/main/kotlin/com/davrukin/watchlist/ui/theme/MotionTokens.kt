package com.davrukin.watchlist.ui.theme

/**
 * Shared animation durations so every transition in the app moves at the same tempo.
 */
object MotionTokens {
    const val QUICK_MILLIS: Int = 150
    const val STANDARD_MILLIS: Int = 300
    const val EMPHASIS_MILLIS: Int = 500
}
